policies:
  - name: my-first-policy
    description: |
      Stops all compute instances that are named "geth-master"
    resource: gcp.instance
    filters:
      - type: value
        key: name
        value: geth-master
        op: in
    actions:
      - type: stop